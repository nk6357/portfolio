{
  "name": "nk6357-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "dev": "npm run generate-projects && vite --configLoader runner",
    "generate-projects": "node --experimental-strip-types scripts/generate-projects.ts",
    "prebuild": "npm run generate-projects",
    "build": "tsc --noEmit && vite build --configLoader runner",
    "preview": "vite preview --configLoader runner",
    "check": "npm run generate-projects && tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
